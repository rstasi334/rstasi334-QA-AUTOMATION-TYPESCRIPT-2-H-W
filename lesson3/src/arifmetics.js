function NumbersAndStrings() {
    const a = Number('5');
    const b = Number('10');
    const c = String(20);
    const d = String(30);
    const e = String('text');

    console.log(a + b, b - a, a + c, d + c, a * b, d / a, a + e);

}

NumbersAndStrings();
